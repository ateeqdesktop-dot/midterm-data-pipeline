# Contributing

أنشئ فرعًا مستقلًا، ثبّت `requirements.txt`، ثم شغّل `PYTHONPATH=src pytest -q` و`ruff check src tests` و`ruff format --check src tests`. لا ترفع بيانات الطلبات الأصلية أو أسرار الاتصال. كل تعديل على قاعدة جودة يجب أن يضيف اختبارًا يثبت السلوك وAudit Trail أو سبب Quarantine.

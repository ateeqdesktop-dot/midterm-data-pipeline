# Security

لا تضع ملفات الطلبات الحقيقية أو كلمات المرور أو مفاتيح MongoDB في Git. ضع بيانات الجامعة داخل `data/` محليًا فقط، واستخدم Environment Variables للاتصال. ترفض قواعد المسار الملفات خارج root والروابط الرمزية، ولا تنفذ الأوامر المسجلة في metadata. للإبلاغ عن مشكلة أمنية، افتح Issue بدون تضمين بيانات حساسة أو تواصل مع مالك المستودع مباشرة.
